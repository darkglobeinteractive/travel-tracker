import React from 'react';

const Spinner = props => {
  return (
    <div className="spinner">
      <div className="wrap">
        [Spinner -- Loading]
      </div>
    </div>
  );
}

export default Spinner;
